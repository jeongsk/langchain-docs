---
title: LangChain 개요
sidebarTitle: 개요
---

:::python
<Warning>
[LangChain v1](/oss/releases/langchain-v1)은 현재 활발히 개발 중이며 프로덕션 환경에서의 사용을 권장하지 않습니다.
* API가 예고 없이 변경될 수 있습니다
* 문서가 불완전하며 변경될 수 있습니다

최신 안정 버전은 [v0 LangChain](https://python.langchain.com/docs/introduction/) 문서를 참고하세요.
</Warning>
:::

:::js
<Warning>
[LangChain v1](/oss/releases/langchain-v1)은 현재 활발히 개발 중이며 프로덕션 환경에서의 사용을 권장하지 않습니다.
* API가 예고 없이 변경될 수 있습니다
* 문서가 불완전하며 변경될 수 있습니다

최신 안정 버전은 [v0 LangChain](https://js.langchain.com/docs/introduction/) 문서를 참고하세요.
</Warning>
:::

LangChain은 LLM 기반 에이전트와 애플리케이션을 구축하는 가장 쉬운 방법입니다. 10줄 미만의 코드로 OpenAI, Anthropic, Google 및 [기타 여러 제공자](/oss/integrations/providers)에 연결할 수 있습니다. LangChain은 사전 구축된 에이전트 아키텍처와 모델 통합을 제공하여 빠르게 시작하고 LLM을 에이전트 및 애플리케이션에 원활하게 통합할 수 있도록 지원합니다.

에이전트와 자율 애플리케이션을 빠르게 구축하려는 경우 LangChain 사용을 권장합니다. 결정론적 워크플로와 에이전트 워크플로의 조합, 고도의 커스터마이징, 정밀한 지연 시간 제어가 필요한 고급 요구사항이 있는 경우 저수준 에이전트 오케스트레이션 프레임워크이자 런타임인 [LangGraph](/oss/langgraph/overview)를 사용하세요.

LangChain [에이전트](/oss/langchain/agents)는 지속 가능한 실행, 스트리밍, 사람 개입, 영속성 등을 제공하기 위해 LangGraph 위에 구축되었습니다. 기본적인 LangChain 에이전트 사용을 위해 LangGraph를 알 필요는 없습니다.

## <Icon icon="download" size={20} /> 설치

:::python
<CodeGroup>
```bash pip
pip install --pre -U langchain
```

```bash uv
uv add --prerelease=allow langchain
```
</CodeGroup>
:::
:::js
<CodeGroup>
```bash npm
npm install langchain@next @langchain/core@next
```

```bash pnpm
pnpm add langchain@next @langchain/core@next
```

```bash yarn
yarn add langchain@next @langchain/core@next
```

```bash bun
bun add langchain@next @langchain/core@next
```
</CodeGroup>
:::

## <Icon icon="wand-magic-sparkles" /> 에이전트 생성

:::python
```python
# pip install -qU "langchain[anthropic]" to call the model

from langchain.agents import create_agent

def get_weather(city: str) -> str:
    """Get weather for a given city."""
    return f"It's always sunny in {city}!"

agent = create_agent(
    model="anthropic:claude-3-7-sonnet-latest",
    tools=[get_weather],
    system_prompt="You are a helpful assistant",
)

# Run the agent
agent.invoke(
    {"messages": [{"role": "user", "content": "what is the weather in sf"}]}
)
```
:::

:::js
```ts
import * as z from "zod";
// npm install @langchain/anthropic to call the model
import { createAgent, tool } from "langchain";

const getWeather = tool(
  ({ city }) => `It's always sunny in ${city}!`,
  {
    name: "get_weather",
    description: "Get the weather for a given city",
    schema: z.object({
      city: z.string(),
    }),
  },
);

const agent = createAgent({
  model: "anthropic:claude-3-7-sonnet-latest",
  tools: [getWeather],
});

console.log(
  await agent.invoke({
    messages: [{ role: "user", content: "What's the weather in Tokyo?" }],
  })
);
```
:::


## <Icon icon="star" size={20} /> 핵심 이점

<Columns cols={2}>
    <Card title="표준화된 모델 인터페이스" icon="arrows-rotate" href="/oss/langchain/models" arrow cta="자세히 알아보기">
        각 제공자는 응답 형식을 포함하여 모델과 상호작용하는 고유한 API를 가지고 있습니다. LangChain은 모델과 상호작용하는 방식을 표준화하여 제공자를 원활하게 전환하고 종속성을 방지할 수 있도록 합니다.
    </Card>

    <Card title="사용하기 쉽고 높은 유연성을 가진 에이전트" icon="wand-magic-sparkles" href="/oss/langchain/agents" arrow cta="자세히 알아보기">
        LangChain의 에이전트 추상화는 쉽게 시작할 수 있도록 설계되어 10줄 미만의 코드로 간단한 에이전트를 구축할 수 있습니다. 하지만 원하는 모든 컨텍스트 엔지니어링을 수행할 수 있을 만큼 충분한 유연성도 제공합니다.
    </Card>

    <Card title="LangGraph 위에 구축" icon="circle-nodes" href="/oss/langgraph/overview" arrow cta="자세히 알아보기">
        LangChain의 에이전트는 LangGraph 위에 구축되었습니다. 이를 통해 LangGraph의 지속 가능한 실행, 사람 개입 지원, 영속성 등을 활용할 수 있습니다.
    </Card>

    <Card title="LangSmith로 디버깅" icon="eye" href="/langsmith/home" arrow cta="자세히 알아보기">
        실행 경로를 추적하고, 상태 전환을 캡처하며, 상세한 런타임 메트릭을 제공하는 시각화 도구를 통해 복잡한 에이전트 동작에 대한 깊은 가시성을 확보하세요.
    </Card>
</Columns>
