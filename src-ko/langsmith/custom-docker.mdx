---
title: Dockerfile 커스터마이징 방법
sidebarTitle: Dockerfile 커스터마이징
---
사용자는 부모 LangGraph 이미지에서 가져온 후 Dockerfile에 추가할 추가 라인 배열을 추가할 수 있습니다. 이를 위해 실행하려는 명령을 `dockerfile_lines` 키에 전달하여 `langgraph.json` 파일을 수정하기만 하면 됩니다. 예를 들어 그래프에서 `Pillow`를 사용하려면 다음 종속성을 추가해야 합니다:

```
{
    "dependencies": ["."],
    "graphs": {
        "openai_agent": "./openai_agent.py:agent",
    },
    "env": "./.env",
    "dockerfile_lines": [
        "RUN apt-get update && apt-get install -y libjpeg-dev zlib1g-dev libpng-dev",
        "RUN pip install Pillow"
    ]
}
```

이렇게 하면 `jpeg` 또는 `png` 이미지 형식을 작업하는 경우 Pillow를 사용하는 데 필요한 시스템 패키지가 설치됩니다.
