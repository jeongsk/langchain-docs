---
title: LangChain 설치하기
sidebarTitle: 설치
mode: wide
---


LangChain 패키지를 설치하려면:

<CodeGroup>
    ```bash pip
    pip install --pre -U langchain
    ```

    ```bash uv
    uv add --prerelease=allow langchain
    ```
</CodeGroup>




LangChain은 수백 개의 LLM과 수천 개의 다른 통합을 제공합니다. 이러한 통합은 독립적인 프로바이더 패키지로 제공됩니다. 예를 들어:

<CodeGroup>
    ```bash pip
    # OpenAI 통합 설치
    pip install --pre -U langchain-openai

    # Anthropic 통합 설치
    pip install --pre -U langchain-anthropic
    ```
    ```bash uv
    # OpenAI 통합 설치
    uv add --prerelease=allow langchain-openai

    # Anthropic 통합 설치
    uv add --prerelease=allow langchain-anthropic
    ```
</CodeGroup>





<Tip>
사용 가능한 통합의 전체 목록은 [통합 탭](/oss/python/integrations/providers/overview)을 참조하세요.
</Tip>

---

<Callout icon="pen-to-square" iconType="regular">
  [Edit the source of this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss/langchain/install.mdx)
</Callout>
