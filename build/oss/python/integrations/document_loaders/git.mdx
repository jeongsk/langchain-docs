---
title: Git
---

>[Git](https://en.wikipedia.org/wiki/Git)은 일반적으로 소프트웨어 개발 중 소스 코드를 협업하여 개발하는 프로그래머 간의 작업을 조정하는 데 사용되는 컴퓨터 파일 세트의 변경 사항을 추적하는 분산 버전 제어 시스템입니다.

이 노트북은 `Git` 저장소에서 텍스트 파일을 로드하는 방법을 보여줍니다.

## 디스크에서 기존 저장소 로드

```python
%pip install -qU  GitPython
```

```python
from git import Repo

repo = Repo.clone_from(
    "https://github.com/langchain-ai/langchain", to_path="./example_data/test_repo1"
)
branch = repo.head.reference
```

```python
from langchain_community.document_loaders import GitLoader
```

```python
loader = GitLoader(repo_path="./example_data/test_repo1/", branch=branch)
```

```python
data = loader.load()
```

```python
len(data)
```

```python
print(data[0])
```

```output
page_content='.venv\n.github\n.git\n.mypy_cache\n.pytest_cache\nDockerfile' metadata={'file_path': '.dockerignore', 'file_name': '.dockerignore', 'file_type': ''}
```

## URL에서 저장소 복제

```python
from langchain_community.document_loaders import GitLoader
```

```python
loader = GitLoader(
    clone_url="https://github.com/langchain-ai/langchain",
    repo_path="./example_data/test_repo2/",
    branch="master",
)
```

```python
data = loader.load()
```

```python
len(data)
```

```output
1074
```

## 로드할 파일 필터링

```python
from langchain_community.document_loaders import GitLoader

# 예: Python 파일만 로드
loader = GitLoader(
    repo_path="./example_data/test_repo1/",
    file_filter=lambda file_path: file_path.endswith(".py"),
)
```

```python

```

---

<Callout icon="pen-to-square" iconType="regular">
    [Edit the source of this page on GitHub.](https://github.com/langchain-ai/docs/edit/main/src/oss/python/integrations/document_loaders/git.mdx)
</Callout>
<Tip icon="terminal" iconType="regular">
    [Connect these docs programmatically](/use-these-docs) to Claude, VSCode, and more via MCP for    real-time answers.
</Tip>
