---
title: LangChain 설치하기
sidebarTitle: 설치
mode: wide
---

import AlphaCalloutPy from '/snippets/alpha-lc-callout-py.mdx';
import AlphaCalloutJS from '/snippets/alpha-lc-callout-js.mdx';


<AlphaCalloutJS />


LangChain 패키지를 설치하려면:



    <CodeGroup>
    ```bash npm
    npm install langchain@next @langchain/core@next
    ```

    ```bash pnpm
    pnpm add langchain@next @langchain/core@next
    ```

    ```bash yarn
    yarn add langchain@next @langchain/core@next
    ```

    ```bash bun
    bun add langchain@next @langchain/core@next
    ```
    </CodeGroup>


LangChain은 수백 개의 LLM과 수천 개의 다른 통합을 제공합니다. 이러한 통합은 독립적인 프로바이더 패키지로 제공됩니다. 예를 들어:



<CodeGroup>
    ```bash npm
    # OpenAI 통합 설치
    npm install @langchain/openai@next
    # Anthropic 통합 설치
    npm install @langchain/anthropic@next
    ```

    ```bash pnpm
    # OpenAI 통합 설치
    pnpm install @langchain/openai@next
    # Anthropic 통합 설치
    pnpm install @langchain/anthropic@next
    ```

    ```bash yarn
    # OpenAI 통합 설치
    yarn add @langchain/openai@next
    # Anthropic 통합 설치
    yarn add @langchain/anthropic@next
    ```

    ```bash bun
    # OpenAI 통합 설치
    bun add @langchain/openai@next
    # Anthropic 통합 설치
    bun add @langchain/anthropic@next
    ```
</CodeGroup>


<Tip>
사용 가능한 통합의 전체 목록은 [통합 탭](/oss/javascript/integrations/providers/overview)을 참조하세요.
</Tip>

---

<Callout icon="pen-to-square" iconType="regular">
  [Edit the source of this page on GitHub](https://github.com/langchain-ai/docs/edit/main/src/oss/langchain/install.mdx)
</Callout>
